n = input()
for _ in xrange(n):
    m = input()
    nums = map(int, raw_input().split())
    l, r = 0, len(nums)-1
    flag = "Yes"
    while l < r:
        if min(nums[l+1], nums[r-1]) > max(nums[l], nums[r]):
            flag = "No"
            break
        l += 1; r -= 1
    print flag

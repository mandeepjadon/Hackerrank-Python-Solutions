# Enter your code here. Read input from STDIN. Print output to STDOUT
m,n=map(int,raw_input().split(" "))
lis=[]
def divideby(x):
    return x/n
for i in range(n):
    lis.append(map(float,raw_input().split(" ")))
z=map(divideby,map(sum,zip(*lis)))
for i in z:
    print "%.2f"%round(i,2)
